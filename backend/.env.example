# Environment (development, staging, production)
# Affects validation warnings and security checks
ENVIRONMENT=development

# Telegram Bot Token (from BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# App Authentication
# You can use either plain text password or bcrypt hash (recommended for production)
#
# Plain text (NOT recommended for production):
# APP_PASSWORD=mySecurePassword123
#
# Bcrypt hash (RECOMMENDED - more secure):
# Generate hash: python backend/scripts/generate_password_hash.py
# APP_PASSWORD=$2b$12$...your_generated_hash...
#
# The app automatically detects which format is used.
APP_PASSWORD=your_secure_password_here

# CSRF Protection Secret (for signing CSRF tokens)
# Generate random secret: python -c "import secrets; print(secrets.token_urlsafe(32))"
CSRF_SECRET=change-this-secret-in-production

# Google Sheets
GOOGLE_SHEETS_ID=18vDpWaCYA1rFhsyb54JhXXtR2b7RVsRplpiWZ93g1N8
GOOGLE_CREDENTIALS_FILE=credentials.json

# Database (optional - uses default path if not set)
# DATABASE_URL=sqlite:///./data/deloculator.db

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:8000,https://your-domain.com
